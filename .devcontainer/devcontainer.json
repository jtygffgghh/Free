{
  "name": "monero-gui-miner",
  "postStartCommand": "chmod +x .devcontainer/setup.sh && bash .devcontainer/setup.sh",
  "mounts": [
    "source=/var/run/docker.sock,target=/var/run/docker.sock,type=bind"
  ],
  "remoteUser": "vscode"
}
